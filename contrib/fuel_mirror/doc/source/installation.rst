============
Installation
============

At the command line::

    $ pip install fuel_mirror

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel_mirror
    $ pip install fuel_mirror
